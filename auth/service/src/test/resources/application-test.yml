spring:
  main:
    banner-mode: off

msa:
  clients:
    jwt:
      baseUrl: ${wiremock.server.base-url}/jwt
    user:
      baseUrl: ${wiremock.server.base-url}/user

logging:
  level:
    root: OFF
