spring:
  main:
    banner-mode: off

msa:
  clients:
    jwt:
      baseUrl: ${wiremock.server.base-url}/jwt
    editor:
      baseUrl: ${wiremock.server.base-url}/editor

logging:
  level:
    root: OFF
